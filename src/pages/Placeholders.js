export function Vouchers() {
  return (
    <div className="page">
      <div className="page-header"><div><h1 className="page-title">Check Vouchers</h1><p className="page-sub">Generate check vouchers and print standard PH-format checks</p></div></div>
      <div className="card" style={{ textAlign: 'center', padding: '48px 24px' }}>
        <div style={{ fontSize: 32, marginBottom: 12 }}>🖨️</div>
        <p style={{ fontSize: 15, fontWeight: 500, marginBottom: 8 }}>Check Voucher module — coming next</p>
        <p style={{ fontSize: 13, color: 'var(--muted)' }}>BDO/BPI-format check printing, voucher generation, check register.</p>
      </div>
    </div>
  )
}
